@page
@model CinemaAplicatieWEB.Pages.Reservations.CreateModel
@{
    ViewData["Title"] = "Create Reservation";
}

<h1>Create Reservation</h1>

<form method="post">
    <div class="form-group">
        <label asp-for="Reservation.ShowtimeId" class="control-label">Select Showtime</label>
        <select asp-for="Reservation.ShowtimeId" class="form-control" asp-items="@(ViewData["Showtimes"] as SelectList)"></select>
    </div>

    <h3>Select Seats</h3>
    <table class="table table-bordered text-center">
        <thead>
            <tr>
                <th>Row</th>
                @for (int i = 1; i <= 11; i++)
                {
                    <th>@i</th> <!-- Afișează numărul coloanei -->
                }
            </tr>
        </thead>
        <tbody>
            @for (char row = 'A'; row <= 'F'; row++)
            {
                <tr>
                    <td>@row</td> <!-- Afișează litera rândului -->
                    @for (int seat = 1; seat <= 11; seat++)
                    {
                        <td>
                            <input type="checkbox" name="SelectedSeats" value="@($"{row}{seat}")" />
                            <label>@($"{row}{seat}")</label>
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>

    <button type="submit" class="btn btn-primary">Reserve</button>
</form>

<div>
    <a asp-page="Index">Back to List</a>
</div>
